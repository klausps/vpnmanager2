# filter fields: id
# recursive: optional field. If enabled, it will update all hubs.
# data fields: trunkX_circuit_id(trunk CIRCUIT_ID), vlan_id(int(1-4094), optional), recursive(bool optional field. If enabled, it will update all hubs.)
- operation: update
  type: l3vpn
  filter:
    id: 1
  data:
    max_prefixes: 400
    circuit_id: UPDATED3
    recursive: True